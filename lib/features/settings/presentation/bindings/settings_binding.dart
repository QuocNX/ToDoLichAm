import 'package:get/get.dart';

/// Binding for SettingsPage dependencies.
class SettingsBinding extends Bindings {
  @override
  void dependencies() {
    // SettingsService is already registered as a global service
  }
}
